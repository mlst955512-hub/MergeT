<B>Git init</B> - to initialize the repo<br>
<B>Git status</B>= showing current status<br>
<B>Git add . </B> = means all files need to add, or we have to include file name in the place of <br>
<B>Git add 1.txt 3.txt </B> – mentioned file need to add <br>
<B>Git Status</B> – show the current status of file which is added and now in green<br>
<B>Git Remote add origin “ Link of Git Repo”</B>= Add the repo of git to <br>
<B>Git Commit -m “ Message for commit”</B> = it will commit the code with message to git </br>
<B>Git Push Origin Master</B><br>
<B>Git Push Origin branch</B> = Where we have to push the code we have to mention according to branch <br>
